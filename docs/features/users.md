# users — CRUD de usuarios
Estado: ✓ terminada | Sprint 1
Endpoints: GET/POST /users, GET/PUT/DELETE /users/{id} (ver api-contract.md)
DB: colección `usuarios`
Regla clave: rolNombre se enriquece en el service desde RoleRepository (no está en el Document)
Frontend: componente lista + formulario con Angular Material
QA: ✓ revisado Sprint 1
